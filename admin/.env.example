# Bedia Admin dashboard — environment template. Copy to .env.local.
# Base URL of the backend API (no trailing slash). Required.
#   local:  http://localhost:9090
#   prod:   https://bedia-claud-backend.adpedia.in
NEXT_PUBLIC_API_BASE_URL=https://bedia-claud-backend.adpedia.in
